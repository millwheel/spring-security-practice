spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/security
    username: season
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  thymeleaf:
    cache: false

logging:
  level:
    org:
      springframework:
        security: DEBUG
